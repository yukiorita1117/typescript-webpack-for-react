{
  "compilerOptions": {
    // コンパイルにおけるオプションを設定
    "sourceMap": true, // ソースマップ は変換後のソースと元のソースを関連付けるファイルであり、ブラウザーが元のソースを再構成して、そのソースをデバッガーに提供できます。
    "target": "es5", // TSはECMAScript 5に変換。出力対象をECMAScript 5仕様のJavaScriptにするということ。
    "module": "es2015", // TSのモジュールはES Modulesとして出力
    // JSXの書式を有効に設定
    "jsx": "react",
    "moduleResolution": "node",
    // コンパイルに使用する組み込みライブラリ
    "lib": ["ES2020", "dom"],
    "allowSyntheticDefaultImports": true,
    "esModuleInterop": true,
    "isolatedModules": true
  }
}
